# ============================================================
# Fetch.ai Agent
# ============================================================

# Seed phrase used to derive the agent's wallet and identity.
# Must be unique — reusing a seed across agents will cause conflicts.
LANGGRAPH_AGENT_SEED=your seed phrase here

# ============================================================
# PostgreSQL (Conversation State Persistence)
# ============================================================

# Connection string for the PostgreSQL database used by LangGraph's
# checkpointer to persist conversation state (cart, user info, etc.)
# across messages. Each conversation is keyed by thread_id.
POSTGRES_CONNECTION_STRING=postgresql://user:password@host:port/database

# ============================================================
# Google Gemini LLM
# ============================================================

# API key for Google Gemini, used for intent classification
# and natural-language item extraction from user messages.
# Get one at: https://aistudio.google.com/apikey
GEMINI_API_KEY=AIza...

# ============================================================
# Square API — Bearer Token (default method)
# ============================================================

# Access token for the Square API. Used for catalog lookups
# and order creation. Obtain from the Square Developer Dashboard
# under your application's credentials tab.
# https://developer.squareup.com/apps
BEARER_TOKEN=EAAAl...

# Square environment to target. Use "sandbox" for development
# and "production" for live transactions.
ENVIRONMENT=sandbox

# ============================================================
# Square OAuth (not needed for now)
#
# These variables are only required if you're running the
# OAuth authorization flow (services/square_oauth.py) to let
# merchants grant access on their behalf. For local dev and
# the current agent setup, the bearer token above is sufficient.
#
# To set up OAuth:
#   1. Go to https://developer.squareup.com/apps
#   2. Create or select an application
#   3. Go to OAuth settings
#   4. Add your redirect URI to the allowed list
# ============================================================

# SQUARE_CLIENT_ID=sandbox-sq0idb-xxxxxxxxxxxxxxxxxxxx
# SQUARE_CLIENT_SECRET=sandbox-sq0csb-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# SQUARE_REDIRECT_URI=http://localhost:3000/square/callback

# ============================================================
# Supabase
# ============================================================

SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
