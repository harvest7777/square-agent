The environment variables `REDIRECT_URI`, `CLIENT_ID`, and `CLIENT_SECRET` can all be found on your Square app's dashboard under the "oAuth" tab:

```
REDIRECT_URI=your_redirect_uri
CLIENT_ID=your_client_id
CLIENT_SECRET=your_client_secret
```

![](2026-02-03-13-35-49.png)
